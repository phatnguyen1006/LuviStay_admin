export enum ApartmentStatus {
    pending,
    accepted,
    all,
}

export enum TagType {
    hotel = "hotel",
    motel = "motel",
    resort = "resort",
    homestay = "homestay",
    other = "other",
}